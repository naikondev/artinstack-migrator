import type { GrapesStyleRule } from "../html-to-grapes/index.js";

/** Parse `<style>` blocks and class rules into Grapes root `styles[]`. */
export function cssToStyles(_css: string): GrapesStyleRule[] {
  return [];
}
